import './stencil.core';
import { EventEmitter } from '@bearer/core';
import '@bearer/ui';
import { TFile } from "./types";
export declare class FeatureAction {
    private listFiles;
    private listDeeperFiles;
    authId: string;
    fileItems: TFile[];
    folders: TFile[];
    data: TFile[];
    storedFiles: TFile[];
    fetchingFiles: boolean;
    input: string;
    path: string[];
    message: string;
    selectedFolder: TFile;
    el: HTMLElement;
    authorizedHandler(event: any): void;
    revokedHandler(event: any): void;
    fetchFiles: () => void;
    fetchFileInFolder: (folder: any) => void;
    filter: (event: any) => void;
    attachFile: (file: any, event: any) => void;
    clearData: () => void;
    sortData: (data: any) => void;
    openModal: () => void;
    save: () => void;
    close: () => void;
    render(): JSX.Element[];
    attachedFiles: TFile[];
    attachedFilesRefIdChanged(event: any): void;
    _loadAttachedFiles: () => void;
    private fetcherRetrieveAttachedFiles;
    _watchAttachedFiles(newValueName: string): void;
    attachedFilesRefId: string;
    private saveFiles;
    filesSaved: EventEmitter;
    files: TFile[];
    filesRefId: string;
    filesSavedWatcher(newValue: any): void;
    constructor();
    readonly SCENARIO_ID: string;
    bearerContext: any;
    setupId: string;
    componentDidLoad(): void;
}
